services:
  - type: web
    name: pdf-translator
    runtime: static
    buildCommand: corepack enable && NODE_ENV=development pnpm install --frozen-lockfile && NODE_ENV=production pnpm run build
    staticPublishPath: ./artifacts/pdf-translator/dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
